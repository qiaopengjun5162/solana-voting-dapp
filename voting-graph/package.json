{
  "name": "voting-graph",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ voting-graph",
    "create-local": "graph create --node http://localhost:8020/ voting-graph",
    "remove-local": "graph remove --node http://localhost:8020/ voting-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 voting-graph",
    "test": "graph test"
  },
  "dependencies": { "@graphprotocol/graph-cli": "0.97.1" }
}
